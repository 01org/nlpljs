(function () {
  window.CP_CONSTANTS = {
    /* icons for services which can serve files */
    SERVICE_ICONS: {
      'Drive': 'cloud'
    },

    /* timeout for images to load within (ms) */
    TIMEOUT: 5000
  };
})();
